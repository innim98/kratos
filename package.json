{
  "name": "kratos",
  "version": "0.1.1",
  "private": true,
  "description": "Agent dashboard with tmux terminal and webview",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "test": "cd server && npm test"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.20"
  }
}
